#include <string>

using namespace std;

string solution(string s) {
    string answer = "";
    if (s.length() % 2 == 1)
        return s.substr(s.length()/2, 1);
    else
        return s.substr(s.length()/2-1, 2);
}
